package paint;
/**
 * Allows the user to close the program with
 * File -> Exit.
 */
public class MenuExit {
	
	public void exit() {
		/**
		 * Closes the program.
		 */
		 System.exit(0);
	}
}
